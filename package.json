{
  "name": "stockflow-sales",
  "version": "2.6.0",
  "private": true,
  "description": "StockFlow shared Firebase RTDB POS, shift closing, catalogue restock and WhatsApp e-invoicing by Muhammad Irfan — no cashier login required",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
