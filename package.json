{
  "name": "stockflow-sales",
  "version": "2.7.0",
  "private": true,
  "description": "StockFlow shared Firebase RTDB POS, resilient catalogue restock, shift closing and WhatsApp e-invoicing by Muhammad Irfan — no cashier login required",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
